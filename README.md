If run all the subnet in the same moment, as the subnet index go up the fail rate is much higher.

Run python retrieve_netuid_data.py for the raw log from bittensor. Then normalize the log into readable JSON with norm_data.py